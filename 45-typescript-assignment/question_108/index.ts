function checkStr(str1: string, str2: string): boolean {
    const string1 = str1.toUpperCase()
    const string2 = str2.toUpperCase()
    return string1 === string2
}
console.log(checkStr('hammad', 'Hammad'))